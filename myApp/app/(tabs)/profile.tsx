import { View, Text } from 'react-native';
export default function profile() {
  return (
    <View style={{ flex:1, justifyContent:'center', alignItems:'center' }}>
      <Text style={{ fontSize:18, fontWeight:'600' }}>profile per testim

      </Text>
    </View>
  );
}
